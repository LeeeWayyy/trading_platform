# Alert Worker Service Dependencies
#
# Versioning Strategy:
# - Version ranges (>=) allow flexibility for Docker builds and minor updates
# - Root requirements.txt has exact pins for development environment
# - This approach balances reproducibility with security patch flexibility
# - Critical: rq is constrained <2.0.0 due to breaking API changes
#
# To regenerate with exact versions: pip freeze > requirements.txt

aiosmtplib>=3.0.0
boto3>=1.35.0
cryptography>=41.0.0
httpx>=0.26.0
fastapi>=0.109.0
hvac>=2.0.0
prometheus-client>=0.19.0
psycopg[binary]>=3.1.0
psycopg-pool>=3.2.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
pyjwt>=2.8.0
python-dotenv>=1.0.0
redis>=5.0.0
rq>=1.16.0,<2.0.0
structlog>=24.0.0
tenacity>=8.2.0
twilio>=8.10.0
